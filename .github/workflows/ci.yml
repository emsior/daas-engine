name: CI

on:
  push:
    branches: [main]
  pull_request: # nigdy pull_request_target: kod z forka nie moze dostac sekretow ani zapisu

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Akcje przypiete do pelnego SHA (tag w komentarzu). Aktualizuje je Dependabot.
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: pip install -e ".[dev]" "ruff==0.16.8"
      - name: OPSEC check
        run: |
          ! git grep -iE "mcq089[@]|bukmache[r]|bettin[g]" -- ':!*.png' ':!*.mp4'
      - name: Lint (ruff)
        run: ruff check --output-format=github .
      - name: Tests (mock mode — no secrets)
        run: pytest -v -p no:cacheprovider
      - name: Smoke API
        run: |
          uvicorn app.main:app --port 8000 &
          sleep 4
          curl -sf localhost:8000/health
          curl -sf -X POST localhost:8000/run -H "Content-Type: application/json" -d '{"pipeline":"ecommerce_demo"}' | grep -q '"MOCK_DATA"'
          curl -sf -F "file=@data/fixtures/client_export_pl.csv" localhost:8000/upload | grep -q '"ready":true'
          curl -sf localhost:8000/reports/ecommerce_demo/latest | grep -q "<table>"

  security:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          persist-credentials: false
          fetch-depth: 0 # gitleaks skanuje cala historie
      - name: Secrets (gitleaks, cala historia)
        run: |
          docker run --rm -v "$PWD:/repo" ghcr.io/gitleaks/gitleaks:v8.30.1 \
            git /repo --redact --no-banner
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - name: Install scanners
        run: pip install "semgrep==1.177.0" "zizmor==1.30.1" # wersje przypiete, podbija Dependabot/recznie
      - name: SAST (semgrep) — blokuje ERROR, WARNING raportuje
        run: |
          semgrep scan --config p/python --config p/owasp-top-ten --metrics off --severity ERROR --error .
          semgrep scan --config p/python --config p/owasp-top-ten --metrics off --severity WARNING . || true
      - name: Audyt workflowow (zizmor)
        run: zizmor --offline --min-severity medium .github/workflows

  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          persist-credentials: false
      - name: Build image
        run: docker build -t daas-engine:ci .
      - name: Run container & smoke
        run: |
          docker run -d --rm --name daas -p 8000:8000 daas-engine:ci
          for i in $(seq 1 20); do curl -sf localhost:8000/health && break || sleep 1; done
          curl -sf -X POST localhost:8000/run -H "Content-Type: application/json" -d '{"pipeline":"cs2_demo"}' | grep -q '"SUCCESS"'
          curl -sf -F "file=@data/fixtures/client_export_pl.csv" localhost:8000/upload | grep -q '"ready":true'
          docker stop daas
